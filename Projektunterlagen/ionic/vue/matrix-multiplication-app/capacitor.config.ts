import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'matrix-multiplication-app',
  webDir: 'dist'
};

export default config;
